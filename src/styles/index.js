// styled-components vscode plugin gives syntax highlighting if we put on css tag
const css = i => i;

export default css`
  .title-bar {
    display: flex;
    justify-content: flex-end;
  }
  .block {
    margin: 5px;
    margin-bottom: 10px;
  }
`;
